# Results directory

This directory stores experiment outputs used to reproduce the manuscript tables and figures.

- `tables/` contains CSV files generated by the experiment notebooks.
- `figures/` contains figures generated from those CSV files and used in the manuscript.

The CSV files in `tables/` are included so that manuscript figures and summary tables can be regenerated without rerunning every experiment from scratch. The notebooks also contain code for regenerating these files.
